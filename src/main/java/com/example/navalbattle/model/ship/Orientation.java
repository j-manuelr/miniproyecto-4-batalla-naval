package com.example.navalbattle.model.ship;

/**
 * The direction in which a {@link Ship}'s cells extend from its origin
 * coordinate.
 */
public enum Orientation {

    /** The ship extends along the same row, increasing columns. */
    HORIZONTAL,

    /** The ship extends along the same column, increasing rows. */
    VERTICAL
}
